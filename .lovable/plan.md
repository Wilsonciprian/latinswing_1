
# Rename band: "Latin Swing Dallas" → "Latin Swing"

Update every user-facing reference so the band shows as **Latin Swing**.

## Changes

- `src/config/site.ts` — `name: "Latin Swing"` (drives nav logo, hero, form subjects)
- `src/pages/About.tsx` — heading and opening sentence use "Latin Swing"
- `index.html` — `<title>`, meta description, author, and Open Graph tags
- Form subject lines in `src/pages/News.tsx` and `src/pages/Contact.tsx` updated to "Latin Swing"

No layout, design, or functionality changes. Email (`latinswingdallas@gmail.com`) and Google Calendar sync remain unchanged.
